<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget list item: a single task/event row.

  Used for: overdue tasks, all-day events, deadline tasks, scheduled tasks,
            and indented frame tasks (indent is applied programmatically via
            setViewPadding on the root view).

  Structure:
    [color bar 3dp] [title + label row]
                    [project chip]       ← hidden when no projectId
                    [time/tag line]

  The color bar uses alignTop/alignBottom on the text content block so it
  always stretches to the full row height (2 lines without chip, 3 with).
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/task_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Text content — drives the row height -->
    <LinearLayout
        android:id="@+id/task_text_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:orientation="vertical"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <!-- Title row: task name + badge (ALL DAY / DUE TODAY / OVERDUE) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tv_task_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Task title"
                android:textColor="@color/widget_text_primary"
                android:textSize="13sp"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Badge: e.g. "ALL DAY", "DUE TODAY", "OVERDUE" — hidden when empty -->
            <TextView
                android:id="@+id/tv_task_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="@color/widget_text_secondary"
                android:textSize="11sp"
                android:textStyle="bold"
                android:letterSpacing="0.06"
                android:layout_marginStart="4dp"
                android:visibility="gone" />

        </LinearLayout>

        <!-- Project chip — hidden when task has no project -->
        <TextView
            android:id="@+id/tv_task_project"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="@color/widget_committed_text"
            android:textSize="10sp"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:paddingTop="1dp"
            android:paddingBottom="1dp"
            android:layout_marginTop="2dp"
            android:background="@drawable/widget_committed_pill"
            android:maxLines="1"
            android:ellipsize="end"
            android:visibility="gone" />

        <!-- Time + tags line -->
        <TextView
            android:id="@+id/tv_task_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="@color/widget_text_secondary"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Color bar — aligned to text content so it always spans the full row height -->
    <View
        android:id="@+id/task_color_bar"
        android:layout_width="3dp"
        android:layout_height="0dp"
        android:layout_alignTop="@id/task_text_content"
        android:layout_alignBottom="@id/task_text_content"
        android:background="#3b82f6" />

</RelativeLayout>
