# dayGLANCE intent protocol

This document has moved. The canonical version of the GLANCE intent protocol now lives in the `@glance-apps/intents` repository:

https://github.com/glance-apps/glance-intents/blob/main/docs/dayglance-intent-protocol.md

The protocol is the universal cross-app contract for the GLANCE family; the historical "dayglance" naming is preserved for continuity. The package `@glance-apps/intents` implements the protocol.
